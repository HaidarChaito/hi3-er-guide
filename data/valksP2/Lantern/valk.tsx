import { valkeryType } from '@/types/Valkery';
import build1 from './build1';
import build2 from './build2';

const Lantern: valkeryType = {
  label: 'Lantern (LDS)',
  image: 'Lantern',
  build: [build1, build2],
};
export default Lantern;
